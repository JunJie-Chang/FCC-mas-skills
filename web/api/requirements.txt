# Web-layer Python dependencies (Phase 1).
# Install with:  python3.13 -m pip install -r web/api/requirements.txt
#
# The agent layer (anthropic, tavily, openai, langgraph, python-docx,
# yfinance, financedatabase, trafilatura, pymupdf, ffmpeg-python) has its
# own implicit set inherited from the existing CLI — those are not
# duplicated here.

fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
sqlalchemy>=2.0
pydantic>=2.10
pydantic-settings>=2.6

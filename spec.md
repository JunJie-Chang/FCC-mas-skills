# Architecture Spec — superseded

> **This document is historical.** It described the original LangGraph
> multi-agent design (`main.py` / `router.py` / `agents/*` / Tavily /
> per-agent LLM calls). That architecture was **removed in the May 2026
> migration to Claude Code Skills** and those modules no longer exist.
>
> For the current architecture, see:
> - **`AGENTS.md`** — layout, build pipeline, conventions
> - **`README.md`** — install & usage
> - **`.claude/skills/fcc-*/SKILL.md`** — the individual skill workflows
> - **`.claude/skills/fcc-shared/SKILL.md`** — shared writing & format rules
>
> The original spec is kept only in git history; it is intentionally not
> reproduced here to avoid describing components that do not exist.

# Required Python packages for FCC-mas skills.
# Installed by install.sh into the user's Python 3.13 environment.

# Word / PPT generation
python-docx==1.2.0
python-pptx==1.0.2

# Financial data
yfinance>=0.2.54
financedatabase>=2.2.0

# OpenAI — STT (gpt-4o-transcribe) + DALL-E 3
openai==2.30.0

# .env loader
python-dotenv==1.2.2

# Web content extraction (podcast skill uses for full-text fetch)
trafilatura>=1.12.0

# Simplified ↔ Traditional Chinese conversion (podcast / translation skills)
opencc>=1.1.6
